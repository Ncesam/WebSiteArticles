root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -gcflags='all=-N -l' -o ./tmp/config_service ./cmd/config/main.go && chmod +x ./tmp/config_service"
  bin = "./tmp/config_service"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]
  delay = 1000
  log = "build.log"
