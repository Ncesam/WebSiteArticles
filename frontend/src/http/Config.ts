import { BotConfig, FormBotConfig } from "@/types/Config"
import { $api } from "./api"


export const startConfig = async (config: BotConfig, data: any) => {
    
}

export const addConfig = async (config: FormBotConfig) => {

}

export const deleteConfig = async (id: number) => {
    
}